import React from 'react';

const Header = (props) => {
    return (
        <header
            className="w-full px-3 py-2"
        >
            Header
        </header>
    )
}

export default Header;
